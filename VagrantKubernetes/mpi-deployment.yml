apiVersion: apps/v1
kind: Deployment
metadata:
  name: mpi-deployment-toni2
  namespace: default
  labels:
    app: mpi
spec:
  replicas: 4
  selector:
    matchLabels:
      app: mpi
  template:
    metadata:
      labels:
        app: mpi
    spec:
      containers:
      - name: mpi
        image: toni1707/repositorytoni:1.0
        ports:
        - containerPort: 22